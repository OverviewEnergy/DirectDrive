# Run Sheet, 31 July 2026

**Today's objective:** BRINGUP Phases 0 to 3, then COMMISSIONING Stage A (dead electrical
check) and Stage B (first light, 5 W). Stage C if daylight and temperature allow.

**Configuration today:** ONE panel, 5 Ω resistor on the cold plate. Not two panels. Not
2 Ω. Voc 40.1 V instead of 80.2 V, 276 W worst case instead of 406 W, and the 5 Ω is the
resistor you actually have.

**Weather:** partly sunny, high 88 °F. Broken cloud is acceptable today because the load
is a resistor and cannot be damaged by an irradiance step. Log the swings, they are free
data for the ramp work. Monday is 70% rain, so do not slip this.

**Hard stop for today:** end of Stage C. Stage D soak and Stage E fault injection need a
scope and a fresh session.

---

## Block 0. Pre-flight blockers, indoors, 30 min

These gate everything below. Clear them before any panel wiring.

| # | Item | Action | Pass |
|---|---|---|---|
| 0.1 | SSR DC rating | Read the label and datasheet. Confirm DC rated, ≥ 100 V, ≥ 20 A | ☐ |
| 0.2 | SSR control current | Bench supply at 5.0 V, DMM in series with control + | < 15 mA ☐ |
| 0.3 | Flow switch substitute | Install a **maintained** toggle switch in slot 3 of the chain. Do NOT jumper the slot closed | ☐ |
| 0.4 | Resistor value and rating | DMM cold. Confirm ≥ 400 W rated and bolted flat to the cold plate with paste | R_cold = ______ Ω ☐ |
| 0.5 | Coolant loop | Pump running, flow present, no leaks, before any power | ☐ |
| 0.6 | Panel Voc | Uncover briefly, DMM across the output. Expect ~40.3 V. Record the serial number | Voc = ______ V ☐ |

If 0.1 fails the SSR is an AC zero-cross part and cannot commutate DC. Today collapses to
Stage A with a manual disconnect in place of the SSR, and you order a DC SSR this
afternoon.

0.3 is a legitimate stand-in because you are testing chain topology, not flow sensing.
Label it. **The real maintained flow switch must be installed before the diode ever sees
power.**

---

## Block 1. LabJack alone, indoors, 25 min

`docker compose stop api` first. Close Kipling before running bringup.py.

### Phase 0, reachable

| Check | Pass | ☐ |
|---|---|---|
| Green and orange LEDs on the Ethernet jack | both lit | ☐ |
| Kipling shows Connected with the static IP, not zeros | | ☐ |
| `ping <ip>` | replies | ☐ |
| Firmware | ≥ 1.0217 | ☐ |

**Set the field static IP now, indoors, over USB, in Kipling:** LabJack 192.168.2.2 /
255.255.255.0 / gw 192.168.2.1, DHCP off. Pi eth0 192.168.2.1/24. Doing this outside in
the sun is how afternoons get lost.

### Phase 1, self-test

**Remove the LJTick-Divider module first.** The loopback needs the AIN3 screw terminal and
the module physically covers it. Reinstall the module after this phase.

```
python3 tools/bringup.py --ip 192.168.2.2 --selftest
```

| Check | Pass | Reading | ☐ |
|---|---|---|---|
| AIN15 ground offset | within ±2.000 mV, with some variation | ______ mV | ☐ |
| DAC0 → AIN3 loopback, 0.5 to 4.5 V | within ~50 mV at every setpoint | | ☐ |

A perfectly static AIN15 value is a fail, not a pass. That is a dead converter.

Reinstall the LJTick-Divider. ☐

---

## Block 2. Interlock chain, indoors, 30 min

**SSR LOAD SIDE PHYSICALLY DISCONNECTED. No panel, no resistor, breaker open.** The tool
requires typing `LOAD DISCONNECTED`.

```
python3 tools/bringup.py --ip 192.168.2.2 --interlock-test
```

| Check | Pass | ☐ |
|---|---|---|
| DAC0 = 0 V | node X ≈ 0 V, FIO1 low | ☐ |
| DAC0 = 5 V, all three switches closed | node X ≈ 5 V, FIO1 HIGH | ☐ |
| Key switch open, others closed | node X ≈ 0 V, FIO1 low | ☐ |
| Lid switch open, others closed | node X ≈ 0 V, FIO1 low | ☐ |
| Flow switch (substitute) open, others closed | node X ≈ 0 V, FIO1 low | ☐ |
| Power-cycle the T7, no software running | DAC0 stays 0 V | ☐ |

One switch at a time. Three separate tests, not one. The entire safety argument rests on
each switch breaking the loop independently in hardware, and that property is either
tested or assumed.

---

## Block 3. HASS, indoors, 20 min

```
python3 tools/bringup.py --ip 192.168.2.2 --zero-hass
```

| Check | Pass | Reading | ☐ |
|---|---|---|---|
| Zero with no current | note the actual value, do not assume 2.500 V | V_zero = ______ V | ☐ |
| 10 turns through the window at 1.2 A | displays 12.0 A ±1% | ______ A | ☐ |
| Reverse the conductor | sign flips | ☐ |

```
python3 tools/bringup.py --ip 192.168.2.2 --hass-turns 10 --hass-zero <V_zero>
```

Write V_zero into the API environment before Block 5. Single-ended wiring has no
reference cancellation, so the zero is a real calibration constant, not a formality.
Power the HASS from clean regulated 5 V, not a switcher tail.

Thermistor, optional today: substitute a precision 10 kΩ resistor and confirm it reads
near 25 °C. That separates wiring and config from the sensor. Skip the three-point
Steinhart-Hart calibration. Temperature is not a safety channel with a cold plate under a
5 Ω resistor.

---

## Block 4. Stage A, outdoors, panel disconnected, 30 min

Rig mounts on one board next to the panel. Short wires. Panel physically disconnected and
covered.

| Check | Pass | Reading | ☐ |
|---|---|---|---|
| Loop continuity, SSR forced on | 5 Ω + wiring, within 0.2 Ω | ______ Ω | ☐ |
| **Node N to LabJack GND** | ≈ 0 V, continuity confirms the tie | ______ V | ☐ |
| Second ground path | none, verify by lifting the tie and losing continuity | ☐ |
| DAQ current with no current flowing | 0.00 A at the measured zero | ______ A | ☐ |
| DAQ voltage | 0.0 V | ______ V | ☐ |
| Flow pulses count with the pump on | FIO0 increments | ☐ |
| SSR load-side continuity follows DAC0 | tracks the command | ☐ |

**Node N to LabJack GND is the check that decides whether every voltage number today is
real.** One tie, nowhere else. Two ties give you a ground loop and a plausible looking
wrong answer.

Do not power the T7 from a USB host. Power bank only. A battery floats, so there is no
mains ground path.

---

## Block 5. Stage B, first light, 5 W, 20 min

`docker compose up -d api`. MODE=ldd. Confirm `/health` shows `influx_reachable: true`.

Panel connected but **heavily covered** so only a little light gets through. Breaker
closed. Arm, then Enable.

| Measurement | Expected | Reading | ☐ |
|---|---|---|---|
| Current, DMM in series | ~1.0 A, LabJack within 1% | DMM ____ / LJ ____ | ☐ |
| Voltage, DMM across the resistor | ~5.0 V, LabJack within 1% | DMM ____ / LJ ____ | ☐ |
| Sign | positive | ☐ |
| Open the lid switch | current to 0 on the DMM | ☐ |
| Software Stop | current to 0 | ☐ |
| Grafana | current, voltage, temp, flow all live | ☐ |

Negative current means the HASS conductor runs backwards through the window. Fix the
conductor, do not fix it in software.

**This is the moment the whole chain is proven.** Real current, correct readings,
interlock functional, data logged. Everything after this is scaling up.

---

## Block 6. Stage C, ~127 W, if time and daylight

Uncover about half.

| Measurement | Expected | Reading | ☐ |
|---|---|---|---|
| Current | ~5.0 A, within 1% of DMM | ______ A | ☐ |
| Voltage | ~25.2 V, within 1% of DMM | ______ V | ☐ |
| Resistor case temp | rising then plateauing, not running away | ______ °C | ☐ |
| Coolant flow under thermal load | steady | ______ LPM | ☐ |
| `loop_dt_s` | stable near 1/SAMPLE_HZ | ______ s | ☐ |

Reference points, refitted to the panel label values (Voc 40.3, Vmp 33.0, Isc 10.23,
Imp 9.70), one panel and 5 Ω:

| Irradiance | V | I | P |
|---|---|---|---|
| 10% | 5.11 V | 1.02 A | 5 W |
| 25% | 12.79 V | 2.56 A | 33 W |
| 50% | 25.51 V | 5.10 A | 130 W |
| 70% | 33.52 V | 6.70 A | 225 W |
| 100% | 37.11 V | 7.42 A | 276 W |

Every point moved by less than 3%, which sits inside both the ±5% panel tolerance and your
1% DMM comparison band. **Today's targets do not change.**

Shade is the power control. No variable supply exists in this rig.

**Stop at Stage C today.** Measure R_hot before you shut down: R_hot = ______ Ω.

---

## Ordered today, not touched today

| Item | Why it blocks | Action |
|---|---|---|
| Maintained-contact flow switch | Hardwired interlock chain is on a substitute | Order today |
| 2 Ω and 4 Ω, ≥ 500 W each, on a ≤ 0.25 °C/W heatsink | DUMMY_LOAD_TEST two-panel session cannot run without them | Order today |
| Hardware analog comparator on HASS Uout | Nothing in the software protects against COD. Microseconds versus milliseconds | Design this week |
| Reverse-parallel clamp diode and TVS above 39 V | Required before the diode sees power | Order today |
| n24i datasheet: absolute max current, COD limit, real wavelength | Direct drive cannot be scheduled without it. The 960 nm vs 878.6 nm discrepancy is unexplained | Email nLight today |
| Ophir Linux USB beta package | Would delete the Windows bridge and the two-clock problem entirely | Email Ophir today |
| DIO_EF_COUNTER_INDEX = 9 | Flow monitoring only, not safety | Confirm against T7 DIO-EF docs |

---

## Notes and anomalies
